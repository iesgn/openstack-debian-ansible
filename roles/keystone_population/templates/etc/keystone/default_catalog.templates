catalog.{{ region }}.identity.publicURL = http://{{ controller_external_ip }}:$(public_port)s/v3
catalog.{{ region }}.identity.adminURL = http://{{ controller_ip }}:$(admin_port)s/v3
catalog.{{ region }}.identity.internalURL = http://{{ controller_ip }}:$(public_port)s/v3
catalog.{{ region }}.identity.name = Identity Service

catalog.{{ region }}.compute.publicURL = http://{{ controller_external_ip }}:8774/v2/$(tenant_id)s
catalog.{{ region }}.compute.adminURL = http://{{ controller_ip }}:8774/v2/$(tenant_id)s
catalog.{{ region }}.compute.internalURL = http://{{ controller_ip }}:8774/v2/$(tenant_id)s
catalog.{{ region }}.compute.name = Compute Service

catalog.{{ region }}.computev21.publicURL = http://{{ controller_external_ip }}:8774/v2.1/$(tenant_id)s
catalog.{{ region }}.computev21.adminURL = http://{{ controller_ip }}:8774/v2.1/$(tenant_id)s
catalog.{{ region }}.computev21.internalURL = http://{{ controller_ip }}:8774/v2.1/$(tenant_id)s
catalog.{{ region }}.computev21.name = Compute Service V2.1

catalog.{{ region }}.volume.publicURL = http://{{ controller_external_ip }}:8776/v1/$(tenant_id)s
catalog.{{ region }}.volume.adminURL = http://{{ controller_ip }}:8776/v1/$(tenant_id)s
catalog.{{ region }}.volume.internalURL = http://{{ controller_ip }}:8776/v1/$(tenant_id)s
catalog.{{ region }}.volume.name = Volume Service

catalog.{{ region }}.volumev2.publicURL = http://{{ controller_external_ip }}:8776/v2/$(tenant_id)s
catalog.{{ region }}.volumev2.adminURL = http://{{ controller_ip }}:8776/v2/$(tenant_id)s
catalog.{{ region }}.volumev2.internalURL = http://{{ controller_ip }}:8776/v2/$(tenant_id)s
catalog.{{ region }}.volumev2.name = Volume Service v2

catalog.{{ region }}.ec2.publicURL = http://{{ controller_external_ip }}:8773/services/Cloud
catalog.{{ region }}.ec2.adminURL = http://{{ controller_ip }}:8773/services/Admin
catalog.{{ region }}.ec2.internalURL = http://{{ controller_ip }}:8773/services/Cloud
catalog.{{ region }}.ec2.name = EC2 Service

catalog.{{ region }}.image.publicURL = http://{{ storage_external_ip }}:9292
catalog.{{ region }}.image.adminURL = http://{{ storage_ip }}:9292
catalog.{{ region }}.image.internalURL = http://{{ storage_ip }}:9292
catalog.{{ region }}.image.name = Image Service

catalog.{{ region }}.network.publicURL = http://{{ controller_external_ip }}:9696
catalog.{{ region }}.network.adminURL = http://{{ controller_ip }}:9696
catalog.{{ region }}.network.internalURL = http://{{ controller_ip }}:9696
catalog.{{ region }}.network.name = Network Service

# catalog.{{ region }}.orchestration.publicURL = http://{{ controller_external_ip }}:8004
# catalog.{{ region }}.orchestration.adminURL = http://{{ controller_ip }}:8004
# catalog.{{ region }}.orchestration.internalURL = http://{{ controller_ip }}:8004
# catalog.{{ region }}.orchestration.name = Orchestration Service

