---

- hosts: all
  roles:
    - role: common

- hosts: controller-nodes
  roles:
    - role: dbserver
    - role: rabbitserver
    - role: keystoneserver

# - hosts: storage-nodes
#   roles: 
#     - role: controller

# - hosts: compute-nodes
#   roles:
#     - role: controller

# - hosts: network-nodes
#   roles:
#     - role: controller